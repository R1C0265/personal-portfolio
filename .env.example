# One .env for everything — no separate backend env needed

# MySQL
DATABASE_URL="mysql://root:08521331@localhost:3306/erickabambe"

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="4d631da287d30582c5a30d9eb77a8426ea0df1a83773d764519753bd06a27422"
JWT_EXPIRES_IN="7d"

# Cookies
COOKIE_SECURE="false"
COOKIE_SAME_SITE="lax"

# Resend (resend.com — free tier 3000 emails/month)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxx"
ADMIN_EMAIL="admin@wakulimited.com"
FROM_EMAIL="onboarding@resend.dev"

# Cloudinary (cloudinary.com — free tier)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Next.js public vars (accessible in browser)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
